import { createQueryKeys } from "#/core/query";

/** Clés de requêtes du module Abonnements (quotas prépayés, backend 089/090). */
export const offresKeys = createQueryKeys("abonnement.offres");
export const souscriptionsKeys = createQueryKeys("abonnement.souscriptions");
