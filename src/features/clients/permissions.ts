import { createQueryKeys } from "#/core/query";

/** Clés de requêtes du module Clients. */
export const clientsKeys = createQueryKeys("client.clients");

/** Taille de page de la pagination client. */
export const CLIENTS_PAGE_SIZE = 12;
