# ── Dépendances ──────────────────────────────────────────────
node_modules

# ── Build / cache de dev ─────────────────────────────────────
dist
dist-ssr
.nitro
.tanstack
.vite
.wrangler
.output
.vinxi
*.tsbuildinfo

# ── Environnement local (NON commités). `.env.example` reste versionné. ──
.env
.env.*
!.env.example
*.local

# ── Tests ────────────────────────────────────────────────────
coverage

# ── Paraglide (i18n compilé — régénéré à chaque build/typecheck) ──
src/paraglide/

# ── Snapshot OpenAPI local (régénéré) — ne PAS committer ─────
scripts/openapi.latest.json

# ── Logs ─────────────────────────────────────────────────────
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── OS ───────────────────────────────────────────────────────
Thumbs.db
Desktop.ini
.DS_Store

# ── Éditeurs ─────────────────────────────────────────────────
.idea/
# On versionne settings.json (formateur/coloration partagés), pas les
# préférences personnelles (launch.json, extensions.json…).
.vscode/*
!.vscode/settings.json

# ── Divers ───────────────────────────────────────────────────
__unconfig*
todos.json
